<p>TOPページ</p>

<% if user_signed_in?  %>
<p>ログインしてるよ</p>
<%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'btn'%>
<% else %>
<p>ログインしてないよ</p>
<%= link_to  "ログイン", new_user_session_path %>
<% end %>

<% if user_signed_in?  %>
<%= current_user.nickname %>
<% end %>

  <%
=begin%>
 <div class='#'>
    <%#= link_to image_tag("", class:"furima-icon"), "/" %>
    <%= form_with(url: shops_search_path, local: true, method: :get, class: "#") do |form| %>
    <%= form.text_field :keyword, placeholder: "キーワードから探す", class: "#" %>
    <button class="#">
      <%#= image_tag "search.png", class:"search-icon" %>
    </button>
    <% end %>
  </div> 
<%
=end%>

  
<h1>
  お店の検索
</h1>
<%= search_form_for @p, url: search_shops_path do |f| %>
  <%= f.label :genre_eq, 'ジャンル' %>
  <%= f.collection_select :genre_eq, @shop_genre, :genre, :genre,  include_blank: '指定なし' %>
  <br>
  <%= f.label :area_eq, '地域' %>
  <%= f.collection_select :area_eq, @shop_area, :area, :area,  include_blank: '指定なし' %>
 <br>
  <%= f.submit '検索' %>
<% end %>



  <% if @shops.length != 0 %>
    <% @shops.each do |shop| %>
      <h3><%= shop.name %></h3>
      <h5><%= shop.text %></h5>
      <h5><%= shop.genre %></h5>
      <h5><%= shop.area %></h5>
    <% end %>
  <% end %>

  <%= link_to  "お店の登録", new_shop_path %>