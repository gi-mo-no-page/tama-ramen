<h1>
  検索結果
</h1>
    <%= paginate(@results) %>

<% if @results.length !=0 %>
  <% @results.each do |result| %>
      <%= image_tag result.image.variant(resize:'300x300'), class: '#' if result.image.attached? %>
      <%= link_to result.name, shop_path(result.id) %>
      <%= result.text %>
      <%= result.category.name %>
  <% end %>
<% else %>
  該当する商品はありません
<% end %>
<br>

    <%= paginate(@results) %>

<%= link_to 'トップページへ戻る', root_path %>